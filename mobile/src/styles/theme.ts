import { StyleSheet } from "react-native";

export const theme = {
  colors: {
    background: "#090C1D",
    backgroundAlt: "#0D132A",
    surface: "rgba(255, 255, 255, 0.08)",
    surfaceElevated: "rgba(255, 255, 255, 0.11)",
    surfaceSoft: "rgba(255, 255, 255, 0.05)",
    surfaceMuted: "rgba(255, 255, 255, 0.035)",
    surfaceStrong: "rgba(255, 255, 255, 0.16)",
    border: "rgba(255, 255, 255, 0.18)",
    divider: "rgba(255, 255, 255, 0.10)",
    borderStrong: "rgba(255, 255, 255, 0.26)",
    primary: "#00D2D9",
    primaryDeep: "#3B8BFF",
    primarySoft: "rgba(0, 210, 217, 0.14)",
    textPrimary: "rgba(246, 250, 255, 0.94)",
    textSecondary: "rgba(218, 228, 241, 0.72)",
    textMuted: "rgba(186, 200, 218, 0.54)",
    textInverse: "#061018",
    success: "#4ade80",
    successSoft: "rgba(74, 222, 128, 0.14)",
    warning: "#fbbf24",
    warningSoft: "rgba(251, 191, 36, 0.14)",
    danger: "#f87171",
    dangerSoft: "rgba(248, 113, 113, 0.14)",
    blueSoft: "rgba(59, 139, 255, 0.14)",
    blueBorder: "rgba(59, 139, 255, 0.30)",
    blueText: "#7DB1FF",
    purpleSoft: "rgba(138, 92, 255, 0.14)",
    purpleText: "#B7A2FF",
    greenSoft: "rgba(74, 222, 128, 0.14)",
    greenText: "#7AF0A4",
    orangeGradientStart: "#FF5DBB",
    orangeGradientEnd: "#00D2D9",
    backButtonBackground: "rgba(255, 255, 255, 0.08)",
    backButtonBorder: "rgba(255, 255, 255, 0.18)",
    backButtonText: "rgba(246, 250, 255, 0.94)",
    brandPink: "#FF5DBB",
    brandPurple: "#8A5CFF",
    brandBlue: "#3B8BFF",
    brandCyan: "#00D2D9",
    shadow: "rgba(0, 0, 0, 0.42)",
    shadowSoft: "rgba(0, 0, 0, 0.24)",
  },
  spacing: {
    xs: 4,
    sm: 8,
    md: 12,
    base: 16,
    lg: 20,
    xl: 24,
    xxl: 32,
    container: 16,
  },
  radius: {
    xs: 4,
    sm: 8,
    md: 12,
    lg: 16,
    xl: 20,
    xxl: 28,
    pill: 9999,
  },
  typography: {
    eyebrow: 11,
    label: 12,
    body: 15,
    section: 18,
    title: 28,
    display: 36,
    hero: 36,
  },
  touch: {
    minHeight: 52,
  },
} as const;

export const sharedStyles = StyleSheet.create({
  screenStack: {
    gap: theme.spacing.md,
  },
  pageHeader: {
    gap: theme.spacing.sm,
    paddingBottom: theme.spacing.xs,
  },
  pageHeaderCentered: {
    alignItems: "center",
    gap: theme.spacing.sm,
  },
  eyebrow: {
    color: theme.colors.primary,
    fontSize: theme.typography.eyebrow,
    fontWeight: "700",
    letterSpacing: 1.2,
    textTransform: "uppercase",
  },
  pageTitle: {
    color: theme.colors.textPrimary,
    fontSize: theme.typography.title,
    fontWeight: "800",
    letterSpacing: -0.3,
    lineHeight: 34,
  },
  displayValue: {
    color: theme.colors.textPrimary,
    fontSize: theme.typography.display,
    fontWeight: "500",
    letterSpacing: 0,
    lineHeight: 44,
  },
  pageCopy: {
    color: theme.colors.textSecondary,
    fontSize: 15,
    lineHeight: 22,
  },
  sectionTitle: {
    color: theme.colors.textPrimary,
    fontSize: theme.typography.section,
    fontWeight: "700",
    letterSpacing: 0,
  },
  card: {
    backgroundColor: theme.colors.surface,
    borderColor: theme.colors.border,
    borderRadius: theme.radius.xl,
    borderWidth: StyleSheet.hairlineWidth,
    padding: theme.spacing.base,
    shadowColor: theme.colors.shadow,
    shadowOffset: { width: 0, height: 10 },
    shadowOpacity: 1,
    shadowRadius: 22,
    elevation: 3,
  },
  cardSoft: {
    backgroundColor: theme.colors.surfaceSoft,
    borderColor: theme.colors.border,
    borderRadius: theme.radius.lg,
    borderWidth: StyleSheet.hairlineWidth,
    padding: theme.spacing.base,
  },
  input: {
    backgroundColor: theme.colors.surfaceElevated,
    borderBottomColor: theme.colors.primary,
    borderBottomWidth: StyleSheet.hairlineWidth,
    borderColor: theme.colors.border,
    borderRadius: theme.radius.md,
    borderWidth: StyleSheet.hairlineWidth,
    color: theme.colors.textPrimary,
    fontSize: theme.typography.body,
    minHeight: 46,
    paddingHorizontal: theme.spacing.md,
    paddingVertical: 12,
  },
  textArea: {
    minHeight: 100,
    paddingTop: 12,
    textAlignVertical: "top",
  },
  primaryButton: {
    alignItems: "center",
    backgroundColor: theme.colors.primary,
    borderRadius: theme.radius.md,
    justifyContent: "center",
    minHeight: 46,
    paddingHorizontal: theme.spacing.base,
    paddingVertical: 12,
  },
  primaryButtonText: {
    color: theme.colors.textInverse,
    fontSize: theme.typography.body,
    fontWeight: "700",
  },
  secondaryButton: {
    alignItems: "center",
    backgroundColor: theme.colors.surface,
    borderColor: theme.colors.borderStrong,
    borderRadius: theme.radius.lg,
    borderWidth: StyleSheet.hairlineWidth,
    justifyContent: "center",
    minHeight: 46,
    paddingHorizontal: theme.spacing.base,
    paddingVertical: 12,
  },
  secondaryButtonText: {
    color: theme.colors.textPrimary,
    fontSize: theme.typography.body,
    fontWeight: "700",
  },
  chip: {
    alignItems: "center",
    backgroundColor: theme.colors.surfaceSoft,
    borderColor: theme.colors.border,
    borderRadius: theme.radius.pill,
    borderWidth: StyleSheet.hairlineWidth,
    justifyContent: "center",
    minHeight: 36,
    paddingHorizontal: 12,
    paddingVertical: 8,
  },
  chipActiveLight: {
    backgroundColor: theme.colors.primarySoft,
    borderColor: theme.colors.primary,
  },
  chipActiveDark: {
    backgroundColor: theme.colors.primary,
    borderColor: theme.colors.primary,
  },
  chipText: {
    color: theme.colors.textSecondary,
    fontSize: theme.typography.label,
    fontWeight: "600",
  },
  chipTextInverse: {
    color: theme.colors.textInverse,
  },
  helperBox: {
    backgroundColor: theme.colors.surfaceSoft,
    borderColor: theme.colors.border,
    borderRadius: theme.radius.lg,
    borderWidth: StyleSheet.hairlineWidth,
    padding: theme.spacing.md,
  },
  helperTitle: {
    color: theme.colors.textPrimary,
    fontSize: theme.typography.label,
    fontWeight: "700",
    marginBottom: theme.spacing.xs,
  },
  helperText: {
    color: theme.colors.textSecondary,
    fontSize: 14,
    lineHeight: 22,
  },
  lineList: {
    borderTopColor: theme.colors.divider,
    borderTopWidth: StyleSheet.hairlineWidth,
  },
  lineRow: {
    borderBottomColor: theme.colors.divider,
    borderBottomWidth: StyleSheet.hairlineWidth,
    paddingVertical: 12,
  },
  lineRowTitle: {
    color: theme.colors.textPrimary,
    fontSize: 15,
    fontWeight: "800",
  },
  lineRowSubtitle: {
    color: theme.colors.textMuted,
    fontSize: 13,
    lineHeight: 19,
    marginTop: 3,
  },
  lineRowValue: {
    color: theme.colors.textPrimary,
    fontSize: 15,
    fontWeight: "900",
    textAlign: "right",
  },
  compactSummaryRow: {
    alignItems: "center",
    borderBottomColor: theme.colors.divider,
    borderBottomWidth: StyleSheet.hairlineWidth,
    borderTopColor: theme.colors.divider,
    borderTopWidth: StyleSheet.hairlineWidth,
    flexDirection: "row",
    justifyContent: "space-between",
    paddingVertical: 14,
  },
  emptyText: {
    color: theme.colors.textMuted,
    fontSize: 14,
    lineHeight: 22,
  },
  successText: {
    color: theme.colors.success,
    fontSize: 14,
    lineHeight: 22,
  },
  warningText: {
    color: theme.colors.warning,
    fontSize: 14,
    lineHeight: 22,
  },
});
